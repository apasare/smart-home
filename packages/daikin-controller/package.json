{
  "name": "@apasare/daikin-controller",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "start": "node dist/test.js",
    "prebuild": "yarn clean",
    "build": "tsc",
    "prewatch": "yarn clean",
    "watch": "tsc -w",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rimraf dist tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "node-fetch": "^2.0.0"
  }
}
